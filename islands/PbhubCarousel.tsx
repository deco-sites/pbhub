import Component from "$store/components/pbhub-carousel/Component.tsx";
import type { Props } from "$store/components/pbhub-carousel/Component.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
